export default function CheckoutForm({item, isTriggered, sendClose}){
    return(
        <section className="`flex flex-col gap-3 fixed z-40 inset-0 bg-white text-neutral">
            <p>Tes</p>
        </section>
    )
}